# GC Futures Prospective Independent Development Cohort Acquisition-First Decision

## 1. Decision record

- Decision date: `2026-08-31`.
- Decision timestamp: `2026-08-31T08:17:34Z`.
- Decision parent and local report commit:
  `3bff80fd12089be17479bac904e39a799d40b6c8`.
- Verified `origin/main` code/governance baseline:
  `6dff5d4ea6e0ebe015cb1fbb7365e6c0b9faa1ef`.
- Program ID: `GC_PROSPECTIVE_INDEPENDENT_DEVELOPMENT_COHORT_V1`.
- Decision type: documentation-only next-research-direction decision.
- Selected direction: `ACQUISITION_FIRST`.
- AI training state: `NOT_STARTED`.
- Final-OOS, integration, execution, and trading authority: `NONE`.
- Global code freeze: `ACTIVE`.

The project will not select another setup on the already consumed development
evidence. It will first freeze a genuinely prospective GC Futures development
cohort, without examining candidate outcomes, labels, model metrics, or final
OOS. This record does not acquire data or authorize a private transaction.

## 2. Exact decision question

The question is what evidence-safe work may follow the terminal cross-segment
resolver and the post-resolver `NOT_READY` training decision.

The answer is to prepare a prospective acquisition contract before choosing or
running another hypothesis. Repeating the resolver, relaxing a closed rule,
choosing another setup on the same sample, or fitting a model now would reuse
consumed evidence and increase selection bias without producing a valid corpus.

## 3. Selected direction and rationale

`ACQUISITION_FIRST` is selected because it is the only active direction that
creates new information while preserving all closed research and the sealed
OOS boundary. The safe fallback remains a full pause.

Acquisition first means:

1. define a prospective time boundary before any new outcome is observed;
2. define source, calendar, session, contract, and completeness requirements;
3. collect immutable raw evidence only under a separately approved manifest;
4. freeze and audit that cohort before selecting one hypothesis; and
5. keep training and final-OOS contact closed until later explicit decisions.

It does not mean buying data, downloading a favorable contract, or searching
until a setup passes.

## 4. Exact documentation-only scope

The only project-governance path introduced by this decision is:

`docs/gc_futures_prospective_independent_development_cohort_acquisition_first_decision.md`

The separately generated readiness-report bundle is explanatory evidence and
does not change source, tests, private inputs, or authority. No Python, test,
fixture, dependency, configuration, private artifact, dataset, feature, label,
model, integration, or execution path may change under this decision.

Three unrelated pre-existing untracked proposal files remain outside scope and
must not be edited, staged, committed, deleted, or used as authority.

## 5. Governing evidence bindings

| Evidence | SHA-256 |
|---|---|
| GC AI strategy and training decision | `237655D31C54133E6E3AE49DB59CD3EC32D5B5D3FC436EE476FA00DCD4629688` |
| Phase-B sweep-reclaim closure decision | `5166E0D14BAA65A2AAFC8E17BE2E1740EC92AFCFCCC4CCED4B60CFF964E36F75` |
| Terminal cross-segment resolver outcome | `107DF12717C0AFC60BA89D1721C02A77E1BD2631BB3C19FA5FFBEEF7330EB67D` |
| Post-resolver pretraining readiness decision | `F344B32A9B3B923EC79F4F96519501D93BF00E4F67EDA1012C8F382991366296` |
| Readiness report source notes | `DACDBB3FEDEF3A96FCF2F11A10E74814E4AEB2CB1654C10BD09ACFCCCAF54188` |
| Readiness report canonical artifact | `3A60953E9216E3AA1481F426CB5CF38656FD8907B7C4664421DB920121CF2BCD` |
| Readiness report portable HTML | `D024FCD0DF9B338D276F069B9C6BF7A72DFD1156E585AC1D57B3F9BC356A8CDE` |

Hash drift invalidates this record rather than silently changing its meaning.
The report's chart is an ordinal evidence-state visualization, not a model or
profitability score.

## 6. Closed evidence and no-rescue boundary

Phase A remains `CLOSED_NEGATIVE`. Phase B remains
`CLOSED_INSUFFICIENT_EVIDENCE`. The terminal resolver remains:

- status `UNKNOWN`;
- reason `CROSS_SEGMENT_CONFIRMATION_UNRESOLVED`;
- resolution count `0`;
- candidate promoted `false`; and
- training, OOS, integration, and trading authority all false.

No Phase A or Phase B candidate, threshold, date, direction, chart review,
outcome, or diagnostic root may be copied, repaired, relabelled, or promoted
into this prospective program.

## 7. Independence contract

The new cohort is independent by decision time, canonical trade date, source
identity, candidate group, and outcome contact. All eligible market sessions
begin after this decision timestamp. Previously downloaded historical files,
closed research derivatives, private diagnostic outputs, and the earlier
independent-corpus attempt are `PRIOR_RESEARCH_ONLY`.

Knowledge that a source exists is not contamination. Any prior inspection of a
candidate, setup rule, label, outcome, PnL, class balance, threshold, or metric
for a prospective row is contamination and excludes its entire atomic group.
Unknown contamination state is `UNKNOWN`; contradiction is `INVALID`.

## 8. Prospective acquisition clock

The proposed raw development-acquisition interval is fixed before outcome
contact as canonical GC trade dates:

`[2026-09-01, 2027-03-01)`.

The interval may not be shifted, shortened after weak results, or extended after
promising results. Data unavailable on a required date remains a recorded gap.
It is never backfilled with a nearby session, a continuous alias, or a manually
selected favorable contract.

No candidate generation or outcome analysis may begin before the interval is
closed, immutable raw acquisition is complete, and a later cohort-freeze audit
passes.

## 9. Raw-evidence-only acquisition purpose

The only allowed future purpose is immutable raw acquisition and lineage. A
future intake may contain exchange-qualified outright GC Futures 5-minute bars,
provider logs, exact source hashes, and authoritative calendar/session evidence.

The intake must not contain candidates, features, H=12 labels, model-ready rows,
class counts, PnL, charts selected by appearance, strategy signals, or inferred
missing data. Raw presence never equals accepted development evidence.

## 10. Source and provider boundary

The intended market-data source is Sierra Chart historical intraday data at
`1 Tick` storage precision, exported as immutable `5M` contract files. A later
manifest must bind the exact provider account-independent metadata, symbol,
exchange qualifier, filename, byte count, row count, SHA-256, capture timestamp,
chart timezone, and acquisition log before any decoding.

This decision does not authorize a subscription, payment, browser purchase,
download, GUI operation, or provider-account change. If Sierra Chart cannot
supply the required evidence, the program stops or receives a new documented
source-equivalence decision; it does not substitute an unverified web series.

## 11. Contract-domain coverage rule

A later acquisition manifest must enumerate every exact exchange-qualified GC
contract source required to determine the active contract and adjacent
comparison at every trade date in Section 8. The roster must be frozen from
objective exchange listing and prior-completed-session volume rules before row
inspection.

Continuous symbols, CFDs, spot XAUUSD, BAR_SUMMARY exports, chart screenshots,
email prose, manually merged files, and symbols lacking contract-month identity
are ineligible. A missing predecessor or successor comparison remains
`UNKNOWN`; it is not inferred from price behavior or open interest observed
later.

## 12. Calendar, timezone, and session contract

Every canonical trade date requires authoritative CME Globex session evidence,
including holiday, early-close, split-session, and reopen states. Source bars
must normalize from the recorded Sierra Chart timezone through a pinned tzdata
version to `America/New_York` and UTC without ambiguous or nonexistent local
times.

Calendar evidence must be acquired and hashed independently from market bars.
Website screenshots may support provenance but cannot replace a structured,
audited session row. Missing or conflicting official evidence is `UNKNOWN` or
`INVALID`; normal hours are never assumed across a holiday.

## 13. Completeness and conservation gates

Before cohort freeze, each admitted source must pass:

- strict schema and numeric validation;
- integer-tick OHLC and nonnegative volume/trade conservation;
- unique ordered effective timestamps;
- exact completed-bar cutoff;
- authoritative expected-slot reconciliation;
- explicit explained gaps with no fill, interpolation, or zero synthesis;
- source-to-calendar and source-to-contract conservation; and
- deterministic replay from immutable bytes.

One malformed source invalidates its atomic transaction. An unavailable but
otherwise well-formed required source yields `UNKNOWN`. Neither can become a
silent exclusion used to improve sample quality.

## 14. Cohort freeze before hypothesis selection

No new setup family, detector combination, time window, threshold, candidate
rule, label horizon, feature, or model may be selected until a later cohort
freeze record establishes the accepted development population without outcome
contact.

After a valid freeze, exactly one independently falsifiable research hypothesis
may be preregistered under a new program/version. The research budget is one
hypothesis. Failure or insufficient evidence closes that hypothesis; it does
not authorize threshold search or another setup on the same cohort.

## 15. Feature, label, and corpus prohibition

This decision does not authorize candidate building, feature extraction, label
generation, chronological partition assignment, preprocessing fit, corpus
publication, training, calibration, inference, or model serialization.

The fixed H=12 label contract from prior design work is not automatically
reused. A later hypothesis decision must bind its own target and prove that the
label interval is fully point-in-time and compatible with purge and embargo.

## 16. Chronology and future OOS boundary

The earlier frozen GCQ26 snapshot and every previously inspected OOS result
remain historical quarantine evidence. They cannot serve as training,
validation, calibration, or a new untouched final OOS for this program.

Any future model program must place its final OOS strictly after all admitted
training, validation, calibration, hypothesis-selection, and threshold decisions.
The earliest future-OOS candidate boundary is canonical trade date
`2027-03-01`; its exact end and sealing contract require a separate decision
before payload acquisition or access.

## 17. Local AI provider contract

Local language models may help summarize public documentation, check manifest
structure, propose test cases, and compare already approved metadata. They have
no authority to ingest raw private market payloads, infer missing bars, choose a
hypothesis from outcomes, create labels, train a model, inspect OOS, issue a
trade direction, or change any project gate.

Every AI-produced statement is advisory and must be verified by deterministic
code or a human-auditable evidence record. No model has trading authority.

## 18. Atomicity and private publication rule

A later private acquisition must use an isolated temporary root, two clean
deterministic metadata runs, byte/object equality, independent audit, and one
atomic rename into a previously absent final root. Failure leaves the final
root absent and removes only the failed temporary roots.

Private raw bytes, manifests containing private paths, provider logs, account
metadata, and future diagnostic outputs remain Git-ignored. They may not be
staged, committed, pushed, uploaded, or supplied to a remote model.

## 19. Status precedence and fail-closed semantics

The future acquisition status order is:

`INVALID > AMBIGUOUS > UNKNOWN > VALID`.

- `VALID` means every independently required acquisition and lineage gate passed.
- `UNKNOWN` means a required fact is unavailable but supplied evidence is valid.
- `AMBIGUOUS` means valid evidence supports mutually exclusive identities.
- `INVALID` means malformed, contradictory, contaminated, or authority-violating evidence.

Only `VALID` may reach a later cohort-freeze decision. `VALID` still does not
grant hypothesis, feature, label, corpus, training, OOS, integration, or trading
authority.

## 20. Prohibited shortcuts

The following are forbidden:

- reusing closed Phase A/B dates as a new independent sample;
- inspecting outcomes before the prospective cohort is frozen;
- downloading only a contract already known to dominate;
- using final-OOS or later bars to choose acquisition coverage;
- filling missing slots, treating missing volume as zero, or changing calendars;
- selecting dates, sessions, directions, or contracts by favorable chart shape;
- changing Section 8 dates after any outcome contact; and
- asking an LLM to manufacture evidence or override an `UNKNOWN` result.

## 21. Reserved next exact task

After this decision is accepted and published under separate GitHub export
authorization, the next single task is one documentation-only proposal for the
prospective acquisition manifest and schedule. That proposal must reserve exact
paths, manifest fields, contract-roster derivation, provider-log capture,
calendar sources, two-run atomicity, rollback, and acceptance tests.

It may not perform the acquisition, create a private root, alter source/tests,
choose a hypothesis, build a dataset/corpus, train a model, inspect OOS, or
integrate trading behavior.

## 22. Exact sequential 48-case acceptance matrix

1. Decision parent and `origin/main` reconcile to their separately stated baselines.
2. The decision timestamp precedes the prospective cohort.
3. Exact one-file governance scope is preserved for this decision.
4. The three unrelated untracked proposals remain untouched.
5. The AI strategy decision hash reconciles.
6. The Phase-B closure hash reconciles.
7. The terminal resolver decision hash reconciles.
8. The post-resolver readiness hash reconciles.
9. All three readiness-report hashes reconcile.
10. The report chart is treated only as ordinal evidence state.
11. Phase A remains `CLOSED_NEGATIVE`.
12. Phase B remains `CLOSED_INSUFFICIENT_EVIDENCE`.
13. Terminal resolver status remains `UNKNOWN`.
14. Terminal resolver resolution count remains zero.
15. No closed candidate or diagnostic root is promoted.
16. Program ID is new and exact.
17. Prospective interval is exactly `[2026-09-01, 2027-03-01)`.
18. Section 8 dates cannot move after outcome contact.
19. Prior historical files remain `PRIOR_RESEARCH_ONLY`.
20. Existing GCQ26 OOS remains historical quarantine evidence.
21. Raw acquisition purpose excludes candidates and outcomes.
22. Sierra Chart is an intended source, not current purchase authority.
23. Exact exchange-qualified contract identities are required.
24. Continuous, CFD, spot, screenshot, and email substitutes are rejected.
25. Contract roster is fixed before row inspection.
26. Missing adjacent comparison remains `UNKNOWN`.
27. Authoritative CME calendar evidence is required per trade date.
28. Timezone and tzdata lineage are immutable.
29. Holiday and split-session states cannot default to normal hours.
30. Strict schema and integer-tick validation are required.
31. Expected-slot and volume conservation are exact.
32. Missing bars are never filled or synthesized.
33. Completed-bar cutoff is point-in-time.
34. Deterministic replay is required.
35. Cohort freeze precedes any new hypothesis.
36. Research budget is exactly one later hypothesis.
37. Candidate, feature, label, and corpus construction remain unauthorized.
38. Training and model serialization remain unauthorized.
39. Final-OOS payload access remains zero.
40. Future OOS begins no earlier than `2027-03-01`.
41. Local AI remains advisory and metadata-only.
42. No AI or runtime receives trading authority.
43. Private acquisition publication must be atomic.
44. Private paths and payloads remain outside Git and remote models.
45. Status precedence is exact and fail-closed.
46. Fresh focused and full public regressions pass.
47. Formatting, exact scope, hashes, and `diff --check` pass.
48. Work stops before private acquisition, training, OOS, integration, and push.

## 23. Audit, promotion, and rollback

Acceptance requires full-content review, exact hash reconciliation, exact
section and case counts, `git diff --check`, cache-disabled focused and full
public regression, and proof that no private or runtime artifact changed.

Fresh cache-disabled acceptance evidence on `2026-08-31` is:

- focused pretraining-corpus regression: `66 passed in 2.31s`;
- full explicit public regression: `2674 passed in 53.81s`;
- report validation/package: `PASS/PASS` with structural verification;
- decision structure: exactly `24` numbered sections and `48` sequential cases;
- final independent-corpus root: absent;
- archived terminal-resolver root: present and unmodified; and
- all seven Section 5 hashes: reconciled.

Portable browser automation could not open the local `file://` report under its
URL security policy. That limitation does not change the report data or package;
it leaves visual interaction unverified and is recorded rather than bypassed.

Promotion of this task means only local commitment and later publication of
this documentation decision. Before commit, rollback is removal of this exact
new file. After commit, rollback requires a bounded revert; Git history and
private evidence are never rewritten.

Any evidence drift, date ambiguity, scope expansion, regression failure,
private-data contact, outcome inspection, or request for training/integration
halts the task and records the blocker without relaxing the decision.

## 24. Final decision and STOP boundary

The exact decision is:

`SELECT_PROSPECTIVE_INDEPENDENT_DEVELOPMENT_ACQUISITION_FIRST_NO_HYPOTHESIS_NO_TRAINING_NO_OOS`

The project remains in evidence acquisition planning. AI training is not in
progress and is not ready. The next valid progress is an exact documentation-only
prospective acquisition manifest/schedule proposal, followed later by a separate
private acquisition authorization if its audit passes.

This decision stops before private data acquisition, provider payment, source
or test implementation, candidate/feature/label/corpus build, preprocessing,
training, validation, calibration, final-OOS access, model inference, integration,
paper trading, live trading, staging, commit, or push.
