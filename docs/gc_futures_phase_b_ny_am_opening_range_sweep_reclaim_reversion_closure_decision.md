# GC Futures Phase B NY-AM Opening-Range Sweep-Reclaim Reversion Closure Decision

## 1. Decision record

- Decision date: `2026-08-24`
- Decision type: documentation-only research closure
- Hypothesis: `GC_NY_AM_OPENING_RANGE_SWEEP_RECLAIM_REVERSION_V1`
- Closure state: `CLOSED_INSUFFICIENT_EVIDENCE`
- Training readiness: `NOT_READY`
- OOS authority: `NONE`
- Trading authority: `NONE`

This record closes the Phase B hypothesis under the currently accepted evidence.
It does not modify source code, tests, private artifacts, calendars, datasets,
features, labels, models, runtime integration, execution behavior, or trading
authority.

## 2. Exact decision question

The bounded question is whether the accepted Phase B evidence justifies another
private feasibility rerun, another evidence-resolution acquisition, promotion to
feature/label construction, OOS inspection, or training.

The answer is `NO`. The accepted evidence supports deterministic closure of this
hypothesis under the current evidence boundary. It does not establish that the
market pattern has no economic edge in every population or future dataset.

## 3. Exact documentation-only scope

The only permitted new artifact is this file:

`docs/gc_futures_phase_b_ny_am_opening_range_sweep_reclaim_reversion_closure_decision.md`

No Python, test, fixture, private-data, calendar, source-manifest, configuration,
integration, training, OOS, or execution file may change. The three unrelated
pre-existing untracked documentation files remain outside scope and unchanged.

## 4. Repository baseline

The binding baseline before this task is:

- HEAD: `a524bc9b477b223feebe20e0e7abcea8b6e8091c`;
- parent: `51430324267b30893a2964487dda3f539a6efa6f`;
- subject: `docs: record GCG26 volume resolution UNKNOWN decision`;
- local `origin/main`: `a524bc9b477b223feebe20e0e7abcea8b6e8091c`; and
- worktree state: clean except for three unrelated pre-existing untracked
  documentation files.

This record does not reinterpret a later commit, private run, or untracked draft.

## 5. Governing decision bindings

The closure is bound to these immutable tracked records and SHA-256 values:

| Record | SHA-256 |
|---|---|
| `docs/gc_futures_phase_b_next_hypothesis_selection_decision.md` | `889CB2DA4FB107AC05A6D9B2395A9FB7E03595C40162339000731B5BAE113AC7` |
| `docs/gc_futures_phase_b_ny_am_opening_range_sweep_reclaim_reversion_v3_failure_decision.md` | `853E8A472C5EEEBC131411999DE1AF05D059C15D7943F98CB309B8EE9228DD91` |
| `docs/gc_futures_phase_b_prospective_three_contract_partition_feasibility_v2_unknown_decision.md` | `4D9CBC66AF764A669DA78F7F63C8F96FC647C85F4163147454A67FD4C11804D9` |
| `docs/gc_futures_phase_b_prospective_three_contract_partition_feasibility_v3_unknown_decision.md` | `39D773C39953D69875C3C7BD0B7149E7A5534C93BDC69E46B79CD2A50102C912` |
| `docs/gc_futures_phase_b_gcg26_completed_session_volume_resolution_unknown_decision.md` | `56EFD7860316805D27A1F416C3D7C3EEDBA65982E89D411C74FA2112D5C90E58` |

The V3 prospective-partition hash is independently recomputed during acceptance;
any mismatch is a STOP condition rather than permission to repair this record
silently.

## 6. Original research-budget contract

The next-hypothesis selection decision allowed exactly one final new setup on the
accepted development sample. It fixed the rules and feasibility gates before
execution, prohibited search-until-positive behavior, and stated that feasibility
failure ends new-setup exploration on that accepted dataset.

The same record locked that FAIL retires the selected V1 without threshold, range,
window, formation, horizon, target, invalidation, or gate rescue. This closure
enforces that precommitted research budget.

## 7. Immutable V3 feasibility result

Two fresh V3 executions produced the same public terminal result:

- status: `AMBIGUOUS`;
- complete candidates: `54`;
- hypothesis decision: `FAIL`;
- publication eligibility: `false`; and
- terminal reason includes `AMBIGUOUS_SWEEP_RECLAIM`.

The requested-date funnel reconciled `64` dates, `63` complete opening ranges, one
attested no-trade date, `54` candidates, eight no-sweep/reclaim dates, one ambiguous
group, and `54` complete outcomes. No silent exclusion or incomplete outcome horizon
was present.

## 8. V3 failure interpretation

The terminal ambiguity was a locked semantic result, not a proven data corruption or
source defect. The same-observation upper-and-lower sweep is atomically ambiguous;
neither direction may win through enumeration, later outcome, proximity, lexical
order, or favorable-subset selection.

Consequently, removing the date, choosing one direction, or changing thresholds
would be post-result hypothesis modification. The original V3 result remains
immutable negative governance evidence.

## 9. Prospective partition V2 evidence

The corrected prospective three-contract partition V2 execution was technically
reproducible but terminal `UNKNOWN`:

- records: `54`, split `27/27` between `GCZ25-COMEX` and `GCG26-COMEX`;
- selected candidate/configuration: `null`;
- promotion authority: `NONE`;
- training readiness: `NOT_READY`; and
- OOS/training/integration contact: `0`.

All 27 GCZ25 records lacked authoritative predecessor coverage. The GCG26 records
were blocked by unresolved calendar or public-builder coverage evidence. V2 selected
nothing and did not prove feasibility PASS or economic failure.

## 10. Prospective partition V3 evidence

The merged-calendar V3 execution resolved the authorized calendar limitation but
remained terminal `UNKNOWN`:

- records: `54`, with every record `UNKNOWN`;
- all 27 GCZ25 records: `INITIAL_PREDECESSOR_COVERAGE_MISSING`;
- all 27 GCG26 records: public builder `UNKNOWN` from required volume evidence;
- selected candidate/configuration: `null`;
- promotion authority: `NONE`; and
- training readiness: `NOT_READY`.

Calendar correction removed the former calendar reasons but did not establish a
qualifying partition, completed-session volume, training readiness, or edge.

## 11. Completed-session volume-resolution evidence

The exact GCG26 volume-resolution diagnostic independently reconstructed and
reconciled:

- `5,130` required evidence rows;
- `4,972` admitted rows;
- `158` `UNKNOWN` rows;
- `0` ambiguous rows;
- `0` invalid rows; and
- all `27` authorized GCG26 candidates affected by at least one unavailable key.

The unavailable rows comprise predecessor coverage and split-session row-completeness
limitations. No missing row was synthesized as zero volume, repaired, reassigned, or
silently excluded.

## 12. Combined evidence conclusion

The evidence layers answer different questions and must not be conflated:

1. the locked setup feasibility run ended in semantic `AMBIGUOUS` and FAIL;
2. prospective training-partition readiness remained `UNKNOWN` after calendar repair;
3. exact GCG26 volume resolution remained `UNKNOWN` for every locked candidate; and
4. no accepted layer produced a selected configuration, promoted dataset, feature,
   label, model, OOS result, or economic-performance conclusion.

Together they justify closure under current evidence, not a universal no-edge claim.

## 13. Exact closure classification

`CLOSED_INSUFFICIENT_EVIDENCE` is a research-governance state local to this record.
It means:

- the hypothesis is no longer eligible for rescue, rerun, or promotion on the
  accepted evidence;
- unresolved evidence remains historically `UNKNOWN`;
- the observed V3 feasibility result remains historically `AMBIGUOUS`/FAIL; and
- current training readiness remains `NOT_READY`.

It must not be relabeled as `PASS`, `VALID`, `NONE`, profitable, unprofitable, BUY,
SELL, or production-ready.

## 14. What this closure does not establish

This record does not establish:

- that sweep-reclaim behavior never exists;
- that it has no edge in another independent population;
- that the 54 outcomes form a tradable or representative sample;
- that missing volume should be zero;
- that another contract can substitute for a missing predecessor;
- that OOS evidence would confirm or reject the hypothesis; or
- that any model should be trained.

Unknown facts remain unknown.

## 15. Hypothesis lifecycle and retirement

The Phase B hypothesis lifecycle is now:

`SELECTED -> IMPLEMENTED_DIAGNOSTICALLY -> PRIVATE_FEASIBILITY_FAIL -> EVIDENCE_RESOLUTION_UNKNOWN -> CLOSED_INSUFFICIENT_EVIDENCE`.

This lifecycle is terminal for the current hypothesis/evidence pair. A future,
genuinely independent research program must receive a new name, preregistration,
data boundary, partition plan, and research budget. It cannot be described as a V1
repair or continuation.

## 16. Immutable code and artifact preservation

Tracked analyzers, tests, checkpoints, proposals, failure decisions, UNKNOWN
decisions, and Git history remain immutable audit evidence. Private roots remain
Git-ignored and must not be staged, published, deleted, overwritten, merged, or
relabelled by this closure.

No dead-code cleanup is authorized. Diagnostic code may remain dormant and
unintegrated so its historical result is reproducible. Removal or archival requires
a separate bounded proposal and independent audit.

## 17. No-rescue and no-rerun boundary

This record forbids, for the closed hypothesis:

- another V3/V4/private rerun;
- changing candidate dates, contracts, predecessor mapping, cutoff, window,
  formation, thresholds, outcomes, precedence, or gates;
- addressing both predecessor and split-session limitations together;
- acquiring evidence solely to turn this result into PASS;
- selecting a favorable direction, date, contract, or subset; and
- using chart appearance, PnL, OOS, or model output to revise closure.

Any such action is research rescue and violates the precommitted budget.

## 18. Training, OOS, and model boundary

Training remains prohibited because there is no accepted promoted dataset, no
eligible three-contract chronological partition, no approved feature table, no
approved label table, no purge/embargo execution, and no untouched OOS authority.

The existing AI strategy training decision remains a design specification only.
Neither local nor remote models may ingest private market payloads, choose trades,
alter evidence, manufacture labels, inspect sealed OOS, or gain execution authority.

## 19. Integration and trading boundary

This closure authorizes no package export, detector aggregation, strategy runtime,
decision trace, engine, broker, paper, or live integration. It creates no
`DECISION_CANDIDATE`, confidence, BUY/SELL direction, risk, entry, exit, sizing,
order, or PnL dependency.

Existing runtime behavior and all-flags-OFF compatibility remain unchanged.

## 20. Next research-direction boundary

The next research question must not be another setup on the same accepted sample.
Before any new implementation or private run, a documentation-only project-level
review must reconcile:

- the closed Phase A and Phase B evidence;
- actual canonical contract and completed-session coverage;
- the minimum independent development/validation/calibration/OOS partitions;
- acquisition provenance and immutable manifests;
- purge/embargo and roll-boundary leakage controls; and
- whether evidence is sufficient to preregister one new research program.

This is a data-and-partition readiness question, not a strategy-selection task.

## 21. Atomicity, freeze, rollback, and promotion

The global code freeze remains active. Acceptance permits only exact-path staging of
this one file, full cached-content review, cached `diff --check`, a one-file local
documentation commit, and a post-commit audit.

Before commit, rollback is removal of only this new file. After commit, rollback
requires a bounded revert; history and private evidence must not be rewritten.
Promotion from this record means only publication of the closure decision under a
later exact GitHub privacy/export authorization.

## 22. Exact sequential 48-case acceptance matrix

1. HEAD, parent, subject, and local `origin/main` reconcile to the baseline.
2. Exact one-file documentation scope is preserved.
3. The three unrelated pre-existing untracked documents remain unchanged.
4. The next-hypothesis selection record hash reconciles.
5. The V3 failure-decision hash reconciles.
6. The prospective-partition V2 UNKNOWN-decision hash reconciles.
7. The prospective-partition V3 UNKNOWN-decision hash reconciles.
8. The GCG26 volume-resolution UNKNOWN-decision hash reconciles.
9. The selected hypothesis name reconciles exactly.
10. The original one-hypothesis research budget is preserved.
11. The no-search-until-positive rule is preserved.
12. The V3 terminal public status remains `AMBIGUOUS`.
13. The V3 hypothesis decision remains `FAIL`.
14. The V3 candidate count remains `54`.
15. The requested-date funnel remains exhaustive with zero silent exclusion.
16. The both-boundary ambiguity semantics remain unchanged.
17. The V3 failure is not reclassified as data corruption.
18. No threshold, date, direction, outcome, or subset rescue occurs.
19. V2 retains exactly `54 UNKNOWN` records and null selection.
20. V2 retains the exact `27/27` contract split.
21. V2 predecessor/calendar/builder limitations remain historically true.
22. V3 retains exactly `54 UNKNOWN` records and null selection.
23. V3 retains 27 predecessor-blocked GCZ25 records.
24. V3 retains 27 volume-blocked GCG26 records.
25. Calendar resolution is not relabelled as feasibility PASS.
26. Volume resolution retains exactly `5,130` required rows.
27. Volume resolution retains exactly `4,972` admitted rows.
28. Volume resolution retains exactly `158 UNKNOWN` rows.
29. Volume resolution retains zero ambiguous and zero invalid rows.
30. All 27 GCG26 candidates remain affected by unavailable evidence.
31. Missing rows are never synthesized as zero volume.
32. Predecessor and split-session limitations remain distinct.
33. Closure is not represented as universal no-edge evidence.
34. `CLOSED_INSUFFICIENT_EVIDENCE` is only a governance state.
35. Training readiness remains exactly `NOT_READY`.
36. OOS contact and authority remain exactly zero/none.
37. Feature, label, model, and training construction remain absent.
38. No detector, strategy, risk, trace, engine, or broker integration occurs.
39. No entry, exit, BUY, SELL, sizing, order, or PnL authority is created.
40. Tracked negative and UNKNOWN evidence remains immutable.
41. Git-ignored private artifacts remain unmodified and unpublished.
42. No new acquisition, private rerun, or new hypothesis is executed.
43. The global code freeze remains active outside this one file.
44. Fresh focused regression passes with cache disabled.
45. Fresh canonical full regression passes with cache disabled.
46. Formatting, section count, case count, and `diff --check` pass.
47. Exact-path staging and one-file local commit scope pass.
48. Work stops before push and before the next readiness proposal.

## 23. Independent audit and regression requirements

Independent acceptance must verify the five governing hashes, all stated counts and
statuses, exactly `24` numbered sections, exactly `48` sequential cases, exact
one-file scope, complete cached contents, cached `diff --check`, final artifact
SHA-256/byte/line counts, and unchanged unrelated state.

Fresh cache-disabled regression must run:

```text
.\venv\Scripts\python.exe -m pytest -q -p no:cacheprovider tests/test_gc_dataset_builder.py
245 passed in 3.00s

.\venv\Scripts\python.exe -m pytest -q -p no:cacheprovider tests
2453 passed in 23.53s
```

These fresh results were produced after the closure document was created and before
staging. Test failure, evidence drift, or a broader diff blocks commitment.

## 24. Final bounded decision and next single task

The exact decision is:

`CLOSE_PHASE_B_NY_AM_SWEEP_RECLAIM_CURRENT_EVIDENCE_INSUFFICIENT_NO_TRAINING_NO_OOS`

The selected Phase B hypothesis is closed under the current accepted evidence. Its
failure and UNKNOWN layers remain immutable; no rerun, acquisition rescue, dataset
promotion, feature/label build, OOS access, training, integration, or trading is
authorized.

After independent acceptance and a local one-file commit, work must STOP before
push. Only after separate exact GitHub privacy/export authorization may the closure
commit be published. The next single task after publication is documentation-only
preparation of:

`docs/gc_futures_post_phase_b_pretraining_data_partition_readiness_decision.md`

That future task may assess data/partition readiness only. It may not implement a
new hypothesis, private run, feature/label build, OOS inspection, training,
integration, or trading.
