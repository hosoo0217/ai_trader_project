# GC Futures Prospective Independent Development Cohort-Open Readiness Decision

## 1. Decision record

- Decision date: `2026-08-31`.
- Published repository baseline:
  `3b3685d9bb6a67d580631352545009fcce21cadb`.
- Program ID: `GC_PROSPECTIVE_INDEPENDENT_DEVELOPMENT_COHORT_V1`.
- Cohort ID:
  `GC_PROSPECTIVE_INDEPENDENT_DEVELOPMENT_COHORT_V1_20260901_20270301`.
- Capability assessed: entry into the already locked prospective cohort clock.
- Private acquisition, provider operation, research, candidate, feature, label,
  corpus, training, final-OOS, integration, execution, and trading authority:
  `NONE`.
- Global code freeze outside this exact file: `ACTIVE`.

This decision records readiness only. It does not create, acquire, inspect,
transform, label, train, evaluate, integrate, schedule, or trade anything.

## 2. Exact documentation-only scope

The only path changed by this decision is:

`docs/gc_futures_prospective_independent_development_cohort_open_readiness_decision.md`

No source, test, fixture, package export, dependency, configuration, private
artifact, provider state, calendar evidence, dataset, corpus, feature, label,
model, runtime, integration, or other documentation path may change.

The three unrelated pre-existing untracked proposal drafts remain untouched.
Acceptance permits exact-path staging, cached audit, one local documentation
commit, and STOP before any additional push.

## 3. Published baseline proof

The exact implementation commit was published before drafting this decision.
Post-push evidence is:

- GitHub `refs/heads/main`:
  `3b3685d9bb6a67d580631352545009fcce21cadb`;
- local `HEAD`:
  `3b3685d9bb6a67d580631352545009fcce21cadb`;
- local `origin/main`:
  `3b3685d9bb6a67d580631352545009fcce21cadb`; and
- ahead/behind: `0/0`.

The implementation commit contains exactly the public validator, its 48-case
synthetic test matrix, and its checkpoint. Publication does not publish or
authorize private evidence.

## 4. Governing evidence hashes

| Governing evidence | SHA-256 |
|---|---|
| Prospective acquisition-first decision | `966521B3FD0E945C8B5DC524FCE2752324D4EC968E4E09C10284851CF3E8455B` |
| Prospective acquisition manifest/schedule proposal | `FA4AF7DDD77D5E75AE82988AEBD5FE98A55B514C2D063C8012AD95CA4335F3B5` |
| Acquisition schema-validator freeze-lift decision | `80915CD80E4D9F6A6850FE728807BCA12C0A2BFAC4D8C3018C223EF9FAF95082` |
| Public acquisition metadata validator | `5714A1FCCCB4516C5D729DAE4C52AAED8279A999466FDA7043547DB288FB4596` |
| Public validator synthetic tests | `072DFCFB479A8803BA68BD2D404B1A4C15B4BE82AE95B1231E07BC2A20BC281A` |
| Validator implementation checkpoint | `85A46A701DC485CCAB0C5035C3903C462BA2799FE3A6579C983FD77A27DC302D` |

Any baseline, hash, program, cohort, date, purpose, or authority drift changes
the decision to STOP rather than being normalized.

## 5. Locked program clock

The program lock timestamp remains exact `2026-08-31T08:17:34Z`. The
prospective canonical trade-date interval remains exact:

`[2026-09-01, 2027-03-01)`.

The cohort becomes eligible only when the authoritative session calendar
assigns canonical GC trade date `2026-09-01`. A computer task, data download,
GUI action, repository mutation, or human button press is not required to
start the clock and cannot move it.

This decision is therefore `READY_TO_ENTER`, not evidence that any eligible
row has already occurred or been acquired.

## 6. Before-open exclusion boundary

Every source timestamp or canonical trade date before `2026-09-01` remains
outside this prospective cohort. Prior historical sources, diagnostic roots,
manual chart reviews, candidate evidence, labels, outcomes, and the existing
GCQ26 OOS snapshot remain prior research or quarantine evidence.

They cannot be relabeled as prospective merely because the new program exists.
Predecessor context may later be preserved only as an explicit context role;
it is never a cohort candidate by default.

## 7. Cohort-open behavior

At cohort open, the authorized behavior is deliberate inactivity:

- do not inspect prospective price paths or outcomes;
- do not choose a hypothesis, threshold, date, contract winner, or direction;
- do not acquire or parse raw rows;
- do not construct candidates, features, labels, partitions, or a corpus;
- do not run a model or compute performance; and
- do not connect any component to trading or execution.

The project preserves the precommitted clock while allowing the market period
to occur independently of research choices.

## 8. Contract-roster boundary

The future roster remains rule-derived from official exchange listing evidence
and accepted completed-session comparison rules. It must contain the required
predecessor context, cohort candidate, and successor context roles and use
exact `GC([GJMQVZ])(\d{2})-COMEX` identities.

No contract may be selected from observed price, volume, setup count, return,
profitability, or model behavior. The public validator can validate supplied
roster metadata but cannot derive or acquire the private roster itself.

## 9. Calendar and trade-date boundary

Canonical membership depends on authoritative CME structured trading hours or
official notices in `America/New_York`, plus the pinned timezone lineage.
Clarification-only evidence cannot establish complete coverage.

Holiday, early halt, close, reopen, pre-open, split-session, DST fold/gap, and
next-business-trade-date semantics remain explicit. Calendar dates, chart
labels, filenames, screenshots, or email summaries cannot substitute for the
canonical GC trade date.

## 10. Metadata checkpoint

The next scheduled governance boundary remains exact
`2026-12-01T00:00:00Z` and is metadata-only. Its permitted subject is future
provider retention/connectivity and official-source availability readiness.

This decision does not create an automation or authorize that future check.
A later exact authorization must define any provider, account-local, browser,
GUI, network, or private-file action. The metadata checkpoint may not inspect
market outcomes or change the cohort clock.

## 11. Cohort-close boundary

The cohort end remains the exclusive canonical trade date `2027-03-01`.
Nothing in this decision shortens, extends, slides, restarts, backfills, or
optimizes the interval.

Future evidence at or after the exclusive end is not admitted as a cohort
candidate. Successor context remains context-only unless another published
decision explicitly defines a later independent cohort before any outcome
contact.

## 12. Private capture window

The reserved future capture window remains exact:

`[2027-03-02T00:00:00Z, 2027-03-09T00:00:00Z)`.

No private acquisition is authorized now. A later transaction may proceed only
after cohort close, published governing evidence, complete official calendar
availability, provider preflight, exact private-run authorization, absent final
and worker roots, and independent atomic two-run controls.

Failure to finish the complete future transaction inside the window remains
`UNKNOWN_ACQUISITION_WINDOW_EXPIRED`; it does not permit a partial cohort or a
moved date.

## 13. Provider and payment boundary

Sierra Chart historical intraday data remains the intended provider. This
decision grants no permission to pay, renew, log in, change an account, operate
the GUI, connect a feed, request a historical download, or export a file.

The future provider preflight still must prove the locked service capability,
`1 Tick` storage, at least `220` historical days, exact exchange-qualified
symbols, and complete message-log metadata. Provider readiness is not inferred
from a prior subscription, screenshot, balance, or successful historical task.

## 14. Validator readiness

The public validator at published commit `3b3685d9...` is ready for synthetic
and later caller-supplied redacted metadata validation. It is pure, deterministic,
keyword-only, immutable, fail-closed, and has no filesystem, network,
current-clock, private-payload, model, training, OOS, integration, or trading
operation.

A `VALID` result means only internally valid raw-acquisition metadata and emits
`VALID_RAW_ACQUISITION_ONLY_NO_RESEARCH_AUTHORITY`. It cannot prove that a
private transaction was authorized or that omitted private evidence does not
exist.

## 15. Status and precedence boundary

Acquisition metadata status precedence remains exact:

`INVALID > AMBIGUOUS > UNKNOWN > VALID`.

Missing evidence is never coerced to zero, false, complete, or valid. Supplied
malformed evidence is still detected when another top-level input is missing.
`NONE` is not a terminal acquisition status, and only `VALID` returns a
manifest.

## 16. Outcome-blindness boundary

Until a later cohort-freeze and research-authority decision, prohibited contact
includes candidate direction, feature values, H=12 labels, future returns,
MAE/MFE, PnL, hit rate, class balance, favorable dates, thresholds, validation
metrics, calibration metrics, model metrics, and final-OOS payloads.

Outcome contact cannot be repaired through exclusion after it influenced roster,
calendar, source, hypothesis, threshold, or interval selection. Confirmed
overlap remains `INVALID_PRIOR_OUTCOME_CONTACT`; unknown history remains
`UNKNOWN_CONTAMINATION_HISTORY`.

## 17. Research-budget boundary

The existing research budget remains exactly one later hypothesis after the
cohort is immutably captured, validated, frozen, and separately promoted for
research. No hypothesis is selected by this decision.

Candidate, feature, label, partition, corpus, training, validation,
calibration, OOS evaluation, model serialization, inference, and integration
authorities remain false.

## 18. Local-AI boundary

Local AI may review published public documentation and synthetic or redacted
metadata schemas. It may not ingest future private raw bytes, account-bearing
provider logs, candidate evidence, labels, outcomes, or final-OOS payloads.

AI output is advisory only and cannot mutate evidence, change validator status,
select contracts, shift dates, grant authority, place orders, or override an
independent audit.

## 19. Privacy and Git boundary

Private paths, raw exports, provider logs, account facts, credentials, payment
details, calendar source payloads, contamination registries, and future
manifests remain outside Git and GitHub.

Only this public documentation decision may be staged and committed in the
current task. Its later publication requires a separate exact GitHub
privacy/export authorization. Current authorization explicitly stops before
that next push.

## 20. No-trading-authority boundary

The program, validator, local AI, future acquisition manifest, and this decision
have no authority to emit or act on BUY/SELL, confidence, position size, entry,
stop, target, risk, order, modification, cancellation, fill, broker, paper, or
live trading instructions.

Runtime strategy, risk, broker, data-feed, order-routing, and execution code
remain unchanged and outside scope.

## 21. Exact 48-case readiness matrix

1. Published remote `main` equals exact implementation commit `3b3685d9...`.
2. Local `HEAD` and `origin/main` equal the same commit.
3. Ahead/behind is `0/0` before this decision draft.
4. The implementation commit contains only its exact three reserved paths.
5. All six Section 4 hashes reconcile.
6. Program ID is exact and unchanged.
7. Cohort ID is exact and unchanged.
8. Program lock remains `2026-08-31T08:17:34Z`.
9. Cohort start remains canonical trade date `2026-09-01`.
10. Cohort end remains exclusive canonical trade date `2027-03-01`.
11. The program clock does not require an operational start action.
12. This decision does not claim that an eligible row already exists.
13. Pre-open historical evidence remains outside the cohort.
14. GCQ26 OOS remains quarantined historical evidence.
15. Predecessor evidence remains context-only.
16. No hypothesis is selected at cohort open.
17. No prospective outcome is inspected.
18. No contract is selected from observed behavior.
19. Exact exchange-qualified GC contract syntax remains required.
20. Official calendar evidence remains required per trade date.
21. Clarification-only calendar evidence remains insufficient.
22. Timezone and DST semantics remain pinned and explicit.
23. Metadata checkpoint remains `2026-12-01T00:00:00Z`.
24. This decision creates no automation for that checkpoint.
25. Cohort close does not move after evidence contact.
26. Capture window remains `[2027-03-02, 2027-03-09)` UTC.
27. No private acquisition is authorized before that window.
28. Expired future capture remains terminal `UNKNOWN`.
29. No provider login, payment, renewal, or GUI operation occurs.
30. Provider readiness is not inferred from prior account state.
31. Public validator version and exact surface remain unchanged.
32. Validator remains pure, deterministic, and no-I/O.
33. `VALID` remains raw-metadata validity only.
34. Status precedence remains fail-closed.
35. Missing evidence is never silently defaulted.
36. Prior outcome overlap remains invalid.
37. Unknown contamination history remains unknown.
38. Research budget remains one later hypothesis.
39. Candidate/feature/label/corpus authority remains false.
40. Training, validation, and calibration authority remains false.
41. Final-OOS access and evaluation authority remains false.
42. Integration and trading authority remains false.
43. Local AI remains public/redacted-metadata advisory only.
44. Private evidence remains outside Git and remote models.
45. Exactly one documentation path changes in this task.
46. Three unrelated untracked drafts remain untouched.
47. Focused and full public regressions pass without private contact.
48. Work stops after one local commit and before the next push.

## 22. Verification and acceptance gates

Acceptance requires:

- exact one-file scope and untouched unrelated drafts;
- published baseline and all Section 4 hashes;
- exact 24 numbered sections and 48 sequential cases;
- no trailing whitespace and cached `git diff --check` PASS;
- focused validator, focused dependency, and full explicit public regression
  with cache disabled; and
- zero private, provider, outcome, OOS, training, integration, or trading
  contact.

Repository-root pytest is not required because explicit `tests/` is the accepted
public suite and avoids traversal of ACL-protected private locations.

Fresh acceptance evidence on `2026-08-31` is:

- published remote/local baseline: exact `3b3685d9...`, ahead/behind `0/0`;
- focused validator plus pretraining dependency regression:
  `114 passed in 0.77s`;
- full explicit public regression: `2722 passed in 38.51s`;
- decision structure: exact `24` numbered sections and `48` sequential cases;
- all six Section 4 hashes: reconciled;
- trailing whitespace: zero;
- tracked source/test/private/runtime changes: zero; and
- unrelated untracked drafts: exact three and unchanged.

## 23. Promotion, rollback, and next valid boundary

Promotion means exact-path staging, cached full-content audit, one local
documentation commit, and STOP. Before commit, rollback is removal of this exact
new file. After commit, rollback requires a bounded revert; broad reset, clean,
checkout, deletion, or history rewrite is forbidden.

After later publication of this decision, ordinary project work still does not
resume. The cohort clock proceeds without intervention. The next scheduled
governance subject is the `2026-12-01T00:00:00Z` metadata checkpoint, and any
action there requires its own then-current exact authorization.

## 24. Final decision and STOP boundary

The exact decision is:

`READY_TO_ENTER_LOCKED_PROSPECTIVE_GC_COHORT_CLOCK_NO_ACQUISITION_NO_RESEARCH_NO_TRAINING_NO_OOS`

The repository is ready to preserve the predetermined prospective clock, not
to acquire or research the cohort now. No payment, provider operation, private
data access, dataset/corpus build, candidate/feature/label generation, training,
final-OOS access, model evaluation, integration, paper trading, or live trading
is authorized.

After independent audit and one local documentation commit, work stops before
push. Publication requires a separate exact GitHub privacy/export authorization.
